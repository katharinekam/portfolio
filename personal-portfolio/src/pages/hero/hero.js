import Header from "../../components/header/header";
import LineGraph from "../../components/header/lineGraph";

const Hero = () => {
  return (
    <section className="hero" id="hero">
      <Header />
      <LineGraph />
    </section>
  );
};

export default Hero;
